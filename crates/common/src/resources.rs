use bevy::prelude::*;

#[derive(Resource, Reflect, Default)]
pub struct MapSize(pub UVec2);
/*pub fn load_config_assets(asset_server: &AssetServer) -> Result<ConfigAssets, BevyError> {
    let config_assets = asset_server.
}*/

